---
// WBR Builder — drop the weekly SEMrush CSV bundle, get the full AI-visibility
// report (scorecards, protect-list, gaps, brand comparison, cited-source mix)
// rendered in-browser and exportable to Excel / PDF. Replaces the manual Excel
// + screenshot auditing step.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>WBR Builder — AI Visibility Report</title>
    <meta name="description" content="Turn the weekly SEMrush AI-visibility CSV exports into a ready WBR: scorecards, gap analysis, brand comparison and cited-source mix." />
    <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
    <style is:global>
      :root {
        --pink: #6366f1; --pink-dark: #4f46e5; --ink: #1d1f2b; --muted: #6b6f80;
        --line: #e7e8ef; --bg: #f6f6fa; --card: #fff; --ok: #15a06a; --warn: #c98a00; --bad: #b3261e;
      }
      * { box-sizing: border-box; }
      body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; background: var(--bg); color: var(--ink); line-height: 1.5; }
      header { background: linear-gradient(120deg, #6366f1 0%, #8b5cf6 100%); color: #fff; padding: 30px 20px 34px; }
      .wrap { max-width: 1180px; margin: 0 auto; padding: 0 20px; }
      header h1 { margin: 0 0 6px; font-size: 26px; font-weight: 800; letter-spacing: -0.02em; }
      header p { margin: 0; opacity: 0.95; font-size: 15px; max-width: 820px; }
      nav.top { margin-top: 14px; display: flex; gap: 10px; }
      nav.top a { color: #fff; text-decoration: none; font-size: 13px; font-weight: 600; background: rgba(255,255,255,0.16); border: 1px solid rgba(255,255,255,0.3); padding: 6px 12px; border-radius: 8px; }
      nav.top a.active { background: #fff; color: var(--pink-dark); }
      .brandbar { display: flex; align-items: center; gap: 9px; margin-bottom: 12px; flex-wrap: wrap; }
      .brandbar .logo { width: 26px; height: 26px; border-radius: 8px; background: rgba(255,255,255,0.22); border: 1px solid rgba(255,255,255,0.4); display: grid; place-items: center; font-size: 13px; }
      .brandbar .wordmark { font-size: 19px; font-weight: 800; letter-spacing: -0.02em; }
      .brandbar .by { font-size: 11.5px; font-weight: 600; opacity: 0.9; }
      main { padding: 26px 0 60px; }
      .card { background: var(--card); border: 1px solid var(--line); border-radius: 14px; padding: 18px; box-shadow: 0 1px 2px rgba(20,20,40,0.04); margin-bottom: 18px; }
      .card h3 { margin: 0 0 4px; font-size: 15px; }
      .card .hint { margin: 0 0 12px; font-size: 12.5px; color: var(--muted); }
      .drop { border: 2px dashed var(--line); border-radius: 12px; padding: 26px; text-align: center; background: #fcfcfe; cursor: pointer; transition: 0.15s; }
      .drop.over { border-color: var(--pink); background: #f4f3ff; }
      .drop p { margin: 6px 0 0; font-size: 13.5px; color: var(--muted); }
      .filelist { margin-top: 12px; display: grid; gap: 6px; }
      .fileitem { display: flex; align-items: center; gap: 10px; font-size: 12.5px; padding: 7px 10px; border: 1px solid var(--line); border-radius: 8px; background: #fff; }
      .fileitem .b { font-weight: 700; }
      .tag { font-size: 10.5px; font-weight: 700; padding: 2px 7px; border-radius: 999px; background: #f0eefc; color: #5b46c9; text-transform: uppercase; letter-spacing: 0.03em; }
      .tag.warn { background: #fdecec; color: var(--bad); }
      .row { display: flex; align-items: center; gap: 16px; margin-top: 18px; flex-wrap: wrap; }
      .field { display: flex; align-items: center; gap: 8px; font-size: 13.5px; color: var(--muted); }
      select, .field input[type=text] { border: 1px solid var(--line); border-radius: 8px; padding: 8px 10px; font-size: 13.5px; background: #fff; }
      button.primary { background: var(--pink); color: #fff; border: none; border-radius: 10px; padding: 12px 22px; font-size: 15px; font-weight: 700; cursor: pointer; }
      button.primary:hover { background: var(--pink-dark); }
      button.primary:disabled { opacity: 0.6; cursor: not-allowed; }
      button.ghost { background: #fff; color: var(--ink); border: 1px solid var(--line); border-radius: 9px; padding: 9px 14px; font-size: 13.5px; font-weight: 600; cursor: pointer; }
      button.ghost:hover { border-color: var(--pink); color: var(--pink); }
      .spinner { width: 16px; height: 16px; border: 2px solid rgba(255,255,255,0.5); border-top-color: #fff; border-radius: 50%; display: inline-block; vertical-align: -3px; margin-right: 8px; animation: spin 0.7s linear infinite; }
      @keyframes spin { to { transform: rotate(360deg); } }
      .alert { margin-top: 16px; padding: 12px 14px; border-radius: 10px; font-size: 13.5px; display: none; }
      .alert.err { background: #fdecec; color: var(--bad); border: 1px solid #f6c9c6; }
      .alert.info { background: #eef6ff; color: #1c4e80; border: 1px solid #c6dcf6; }
      #results { display: none; }
      .results-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; flex-wrap: wrap; margin-bottom: 14px; }
      .results-head h2 { margin: 0; font-size: 20px; }
      .actions { display: flex; gap: 8px; flex-wrap: wrap; }
      .tabbar { display: flex; gap: 6px; margin: 4px 0 18px; border-bottom: 2px solid var(--line); }
      .tab { background: none; border: none; border-bottom: 2px solid transparent; margin-bottom: -2px; padding: 9px 14px; font-size: 14px; font-weight: 700; color: var(--muted); cursor: pointer; }
      .tab.active { color: var(--pink-dark); border-bottom-color: var(--pink); }
      .tab:disabled { opacity: 0.4; cursor: not-allowed; }
      .kpis { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 12px; margin-bottom: 14px; }
      .kpi { background: #fff; border: 1px solid var(--line); border-radius: 12px; padding: 13px 15px; }
      .kpi .v { font-size: 22px; font-weight: 800; letter-spacing: -0.02em; }
      .kpi .l { font-size: 11.5px; color: var(--muted); margin-top: 2px; }
      .highlights { background: #f4f3ff; border: 1px solid #ddd9fb; border-radius: 12px; padding: 14px 18px; }
      .highlights h3 { color: var(--pink-dark); }
      ul.hl { margin: 6px 0 0; padding-left: 18px; }
      ul.hl li { font-size: 13.5px; margin-bottom: 7px; }
      .sec { margin-bottom: 22px; }
      .sec h3 { font-size: 16px; margin: 0 0 3px; }
      .sec .sub { font-size: 12.5px; color: var(--muted); margin: 0 0 10px; }
      .callout { margin: 10px 0 0; padding: 9px 12px; background: #fff8e6; border: 1px solid #f3e0b0; border-radius: 9px; font-size: 13px; color: #6b5400; }
      .stories .storygrid { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
      @media (max-width: 820px) { .stories .storygrid { grid-template-columns: 1fr; } }
      .story { border: 1px solid var(--line); border-radius: 14px; padding: 15px 17px; background: #fff; }
      .story-nykaa { background: #f3fbf6; border-color: #b9e6cd; }
      .story-comp { background: #fff6f4; border-color: #f6ccc4; }
      .story .story-h { font-size: 13px; font-weight: 800; margin-bottom: 6px; }
      .story p { margin: 0; font-size: 13.5px; line-height: 1.65; color: #2a2d3a; }
      .notesgrid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 14px; }
      @media (max-width: 820px) { .notesgrid { grid-template-columns: 1fr; } }
      .notesgrid .nf { flex-direction: column; align-items: stretch; gap: 5px; }
      .notesgrid textarea { width: 100%; border: 1px solid var(--line); border-radius: 9px; padding: 9px 11px; font-size: 13px; font-family: inherit; resize: vertical; }
      .notesbox { margin: 0 0 16px; padding: 12px 15px; background: #eef6ff; border: 1px solid #c6dcf6; border-radius: 12px; font-size: 13.5px; white-space: pre-wrap; }
      .notesbox .nb-h { font-weight: 800; font-size: 12px; color: #1c4e80; margin-bottom: 4px; }
      /* Cross-brand hero cards */
      .brandcards { display: grid; grid-template-columns: repeat(auto-fit, minmax(170px, 1fr)); gap: 12px; margin-bottom: 16px; }
      .brandcard { background: #fff; border: 1px solid var(--line); border-radius: 14px; padding: 14px 16px; }
      .brandcard.win { border-color: #b9e6cd; background: #f3fbf6; }
      .brandcard .bc-name { font-size: 13px; font-weight: 800; letter-spacing: -0.01em; }
      .brandcard .bc-num { font-size: 26px; font-weight: 800; letter-spacing: -0.02em; margin-top: 2px; }
      .brandcard .bc-cap { font-size: 11px; color: var(--muted); }
      .brandcard .bar { height: 7px; background: #eceaf6; border-radius: 99px; overflow: hidden; margin: 9px 0 7px; }
      .brandcard .bar span { display: block; height: 100%; background: linear-gradient(90deg, #6366f1, #8b5cf6); border-radius: 99px; }
      .brandcard.win .bar span { background: linear-gradient(90deg, #15a06a, #38c172); }
      .brandcard .bc-sub { font-size: 11px; color: var(--muted); }
      /* Metric toggle */
      .metric-toggle { display: flex; gap: 8px; align-items: center; flex-wrap: wrap; margin: 6px 0 16px; }
      .mt { background: #fff; border: 1px solid var(--line); border-radius: 999px; padding: 7px 16px; font-size: 13px; font-weight: 700; color: var(--muted); cursor: pointer; }
      .mt.active { background: var(--pink); border-color: var(--pink); color: #fff; }
      .mt-hint { font-size: 11.5px; color: var(--muted); }
      /* Cross-brand table emphasis */
      td .win, .win { color: #0f8a59; font-weight: 800; }
      tr.subtotal-row td { background: #faf9ff; font-weight: 700; border-top: 1px solid var(--line); }
      tr.grand-row td { background: #efeefc; font-weight: 800; font-size: 13.5px; }
      .table-wrap { overflow-x: auto; border: 1px solid var(--line); border-radius: 12px; background: #fff; }
      table { border-collapse: collapse; width: 100%; font-size: 13px; }
      th, td { text-align: left; padding: 9px 12px; border-bottom: 1px solid var(--line); white-space: nowrap; }
      th { background: #fafafe; font-size: 11px; text-transform: uppercase; letter-spacing: 0.03em; color: var(--muted); }
      td.num, th.num { text-align: right; font-variant-numeric: tabular-nums; }
      tr:last-child td { border-bottom: none; }
      tr.lead td { background: #f3f8f4; }
      .st-ok { color: var(--ok); font-weight: 600; }
      .st-warn { color: var(--warn); font-weight: 600; }
      .st-bad { color: var(--bad); font-weight: 600; }
      .prio-High { color: var(--bad); font-weight: 700; }
      .prio-Medium { color: var(--warn); font-weight: 700; }
      .prio-Low { color: var(--muted); font-weight: 600; }
      details.review summary { cursor: pointer; font-weight: 600; font-size: 13.5px; }
      footer { text-align: center; color: var(--muted); font-size: 12.5px; padding: 24px; }
      .toast { position: fixed; bottom: 22px; left: 50%; transform: translateX(-50%); background: var(--ink); color: #fff; padding: 10px 18px; border-radius: 10px; font-size: 13.5px; opacity: 0; transition: 0.2s; pointer-events: none; }
      .toast.show { opacity: 1; }
      @media print {
        header, nav.top, #inputs, .actions, .tabbar, .metric-toggle, footer, .review { display: none !important; }
        /* Print every metric block so the saved PDF has all metrics */
        .metric-block { display: block !important; }
        body { background: #fff; }
        .card { box-shadow: none; border: none; padding: 0; }
        #results { display: block !important; }
        .table-wrap { overflow: visible; }
        .sec { page-break-inside: avoid; }
      }
    </style>
  </head>
  <body>
    <header>
      <div class="wrap">
        <div class="brandbar">
          <span class="logo">📊</span>
          <span class="wordmark">CiteRank WBR</span>
          <span class="by">By Rudra Kasturi Inc</span>
        </div>
        <h1>Weekly AI-Visibility Report Builder</h1>
        <p>
          Drop this week's SEMrush exports (<code>brand_topics</code>, <code>gap_topics</code>,
          <code>sources</code> for each brand). The builder auto-categorizes every topic and
          produces the full report — summary scorecard, category scorecard, topics to protect,
          gap analysis, brand comparison and cited-source mix — ready to export to Excel or PDF.
        </p>
        <nav class="top">
          <a href="/">✍️ Topic Engine</a>
          <a href="/audit">🔎 AEO Auditor</a>
          <a href="/wbr" class="active">📊 WBR Builder</a>
        </nav>
      </div>
    </header>

    <main>
      <div class="wrap">
        <div id="inputs">
          <div class="card">
            <h3>1 · Upload the weekly SEMrush CSV bundle</h3>
            <p class="hint">Filenames are auto-detected for brand (Nykaa / Amazon / Myntra / Tira / Flipkart) and type. Drop everything for one vertical at once.</p>
            <div class="drop" id="drop">
              <strong>Drop CSV files here</strong> or click to browse
              <p>SEMrush exports (.csv) — and optionally your master tracker (.xlsx)
                 to show the Weekly Theme Tracking table.</p>
              <input type="file" id="fileInput" accept=".csv,.xlsx,.xls,.numbers" multiple hidden />
            </div>
            <div class="filelist" id="filelist"></div>
            <div class="row">
              <label class="field">Vertical
                <select id="vertical">
                  <option value="beauty">Beauty</option>
                  <option value="fashion">Fashion</option>
                </select>
              </label>
              <label class="field" title="Use Claude to classify topics the keyword rules couldn't place. Requires server API key.">
                <input type="checkbox" id="useClaude" /> Claude fallback for un-categorized topics
              </label>
              <label class="field">Report label
                <input type="text" id="reportLabel" placeholder="e.g. Nykaa Beauty · Wk 24" />
              </label>
              <label class="field">Week date
                <input type="date" id="weekKey" />
              </label>
              <label class="field" title="Remember this week so next week can show week-over-week change.">
                <input type="checkbox" id="saveToHistory" checked /> Save to history
              </label>
            </div>
            <div class="notesgrid">
              <label class="field nf">Notes for SEMrush Report tab
                <textarea id="notesSemrush" rows="2" placeholder="Your commentary / instructions — shows at the top of the SEMrush tab and in its exports."></textarea>
              </label>
              <label class="field nf">Notes for Weekly Tracking tab
                <textarea id="notesTracking" rows="2" placeholder="Your commentary / instructions — shows at the top of the Weekly Tracking tab and in its exports."></textarea>
              </label>
            </div>
            <div class="row">
              <button class="primary" id="genBtn">Generate report</button>
              <button class="ghost" id="histBtn" type="button">View saved weeks</button>
            </div>
            <div id="histPanel" style="display:none;margin-top:14px"></div>
            <div class="alert err" id="errBox"></div>
            <div class="alert info" id="infoBox"></div>
          </div>
        </div>

        <div id="results">
          <div class="results-head">
            <h2 id="rTitle">AI-Visibility Report</h2>
            <div class="actions" id="actSemrush">
              <button class="ghost" id="xlsxBtn">⬇ Download Excel</button>
              <button class="ghost" id="pdfBtn">🖨 Print / Save PDF</button>
            </div>
            <div class="actions" id="actTracking" style="display:none">
              <button class="ghost" id="xlsxTrackBtn">⬇ Download Excel</button>
              <button class="ghost" id="pdfTrackBtn">🖨 Print / Save PDF</button>
            </div>
          </div>
          <div class="tabbar">
            <button class="tab active" data-tab="semrush" id="tabSemrush">📊 SEMrush Report</button>
            <button class="tab" data-tab="tracking" id="tabTracking">📈 Weekly Tracking</button>
          </div>
          <div id="reportSemrush" class="tabpane"></div>
          <div id="reportTracking" class="tabpane" style="display:none"></div>
        </div>
      </div>
    </main>

    <footer>CiteRank WBR · Source: SEMrush AI Visibility · By Rudra Kasturi Inc</footer>
    <div class="toast" id="toast"></div>

    <script type="module" src="/scripts/wbr-client.js"></script>
  </body>
</html>
